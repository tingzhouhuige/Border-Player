# Border Player MSIX 安装说明

如果你发布的是 MSIX 包，请先导入随包证书，再安装 `border_player.msix`。

1. 右键 `border_player.msix`，打开属性。
2. 进入“数字签名”页。
3. 选择签名并查看证书。
4. 点击“安装证书”。
5. 存储位置选择“本地计算机”。
6. 将证书放入“受信任的根证书颁发机构”。
7. 双击 `border_player.msix` 安装。
